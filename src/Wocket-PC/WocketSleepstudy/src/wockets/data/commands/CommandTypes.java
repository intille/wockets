package wockets.data.commands;

public enum CommandTypes {	
	 GetBatteryLevel,
     GetBatteryPercent,
     GetPacketCount,
     ResetBluetooth,
     GetSensorSentivity,
     SetSensorSentivity,
     GetCalibrationValues,
     SetCalibrationValues,
     GetSamplingRate,
     SetSamplingRate,
     GetAliveTimer,
     SetAliveTimer,
     GetPowerDownTimer,
     SetPowerDownTimer,
     ResetWocket,
     Alive,
     PausePacket,
     ResumePacket,
     GetRadioTransmissionMode,
     SetRadioTransmissionMode,
     GetBatteryCalibration,
     SetBatteryCalibration,
     GetHardwareVersion,
     GetFirmwareVersion,
     GetTCT,
     SetTCT,
     SetWocketTransmissionMode,
     ACK,
     SetLED,
     ShutdownWocket
}
